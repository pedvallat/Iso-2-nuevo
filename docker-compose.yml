version: '3.8'

services:
  novnc:
    image: theasp/novnc
    container_name: novnc
    ports:
      - "8081:8080"
    environment:
      - RUN_FLUXBOX=yes
      - RUN_XTERM=yes
    restart: unless-stopped
